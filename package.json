{
  "homepage": "https://Gu234.github.io/gyro-gyro",
  "dependencies": {
    "gh-pages": "^2.2.0"
  },
  "scripts": {
    "build": "mkdir -p build && cp *.* build/",
    "predeploy": "yarn run build",
    "deploy": "gh-pages -d build"
  }
}